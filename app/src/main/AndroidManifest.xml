<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.burakcanduzcan.currencylisterx">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CurrencyListerXApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_currency_bitcoin_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_currency_bitcoin_24"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurrencyLister">
        <activity
            android:name="com.burakcanduzcan.currencylisterx.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>